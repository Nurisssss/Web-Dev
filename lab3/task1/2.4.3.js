//const birthday = '18.04.1982';

//const age = someCode(birthday);

// We should use upper case for birthday because it is known at the start, 
// but age is calculated from birthday, so it is better if do not use uppercase for age.