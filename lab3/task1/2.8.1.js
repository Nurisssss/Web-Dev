let a = 1, b = 1;
// a = 1, b = 1, before any operations
let c = ++a; // answer: c = 2, because it first adds 1 to a
let d = b++; // answer: d = 1, because it first returns the value of b
// both a and b become 2 after incremention